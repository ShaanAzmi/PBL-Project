numpy>=1.24
pandas>=2.0
scikit-learn>=1.2
matplotlib>=3.7
seaborn>=0.12
tqdm>=4.65
pillow>=9.5
opencv-python>=4.8
torchmetrics>=0.11
tensorboard>=2.12
# NOTE: PyTorch (torch, torchvision, torchaudio) is NOT pinned here because GPU-enabled
# wheels depend on the CUDA version installed. Use the `setup_env.ps1` script to install
# a matching CUDA-capable build, or install manually following https://pytorch.org
